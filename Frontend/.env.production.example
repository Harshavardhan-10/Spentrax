# Production build-time environment template — copy this file to .env.production
# locally, or set VITE_API_URL in your host's build environment.
# Only variables prefixed with VITE_ are exposed to the browser bundle.

VITE_API_URL=https://spentrax-api.onrender.com